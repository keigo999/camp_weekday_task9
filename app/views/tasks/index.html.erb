<h1>タスク一覧</h1>

<div>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> <%= task.created_at.strftime("%Y年%m月%d日")  %></li>
  <% end %>
</div>
<%= link_to "出品する", new_task_path %>
<%= link_to "非表示一覧" , tasks_hide_path %>
